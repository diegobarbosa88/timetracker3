// Archivo de instrucciones para despliegue estático
// deploy-static.js

/**
 * Este script proporciona instrucciones para generar y desplegar
 * una versión estática de la aplicación TimeTracker.
 * 
 * Sigue estos pasos para generar una versión estática:
 * 
 * 1. Asegúrate de tener instalado Node.js y npm
 * 2. Ejecuta 'npm install' para instalar dependencias
 * 3. Ejecuta 'npm run build' para generar la versión estática
 * 4. La versión estática se generará en la carpeta 'out'
 * 5. Sube el contenido de la carpeta 'out' a cualquier hosting estático
 *    (Netlify, GitHub Pages, Vercel como sitio estático, etc.)
 */

console.log('Este es un archivo de instrucciones para despliegue estático.');
console.log('Sigue los pasos descritos en los comentarios de este archivo.');
